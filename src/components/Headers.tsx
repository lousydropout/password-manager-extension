import { HStack } from "@chakra-ui/react";
import { FC } from "react";

const Headers: FC = () => {
  return (
    <HStack>
      {/* Credentials */}

      {/* Sync / Create new cred */}

      {/* Refresh */}

      {/* Settings */}
    </HStack>
  );
};
export { Headers };
